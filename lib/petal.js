(function() {
  var $, api_base, load_comments, root;

  $ = jQuery;

  $.support.cors = true;

  api_base = "https://api.github.com/repos/";

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  root.petal = {
    repo: "user/repo",
    issue_id: 1,
    init: function(repo, issue_id) {
      this.repo = repo;
      this.issue_id = issue_id;
      this.api_url = api_base + this.repo + "/issues/" + this.issue_id + "/comments";
      return load_comments();
    }
  };

  load_comments = function() {
    return $.getJSON(petal.api_url + "?callback=?", function(response) {
      var com, data, _i, _len, _results;
      data = response.data;
      _results = [];
      for (_i = 0, _len = data.length; _i < _len; _i++) {
        com = data[_i];
        _results.push(alert(com.body));
      }
      return _results;
    });
  };

}).call(this);

// Generated by CoffeeScript 1.5.0-pre
